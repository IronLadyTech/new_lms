node_modules
dist
.env
.env.local
.env.staging
functions/.env
functions/.env.local
.secrets/
*.sa.json
*-sa.json
*.log
.DS_Store
test-results
playwright-report
.gstack/

# Firebase CLI deploy cache — regenerated on every deploy.
.firebase/

# Local-only test credentials used for capturing walkthrough screenshots.
# Never commit these.
*.local
docs/screenshots/.test-creds*

# Saved Playwright auth state (contains live tokens).
e2e/.auth-state.local.json

# Firestore emulator debug log.
firestore-debug.log
firebase-debug.log
ui-debug.log

# Generated standalone copy of the report for opening in a browser.
LMS_TEST_REPORT.view.html

# Generated deliverable — rebuild with npm run report:pdf
Iron Lady LMS — Quality Assessment.pdf
